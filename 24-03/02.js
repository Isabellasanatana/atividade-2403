let  numeros = [1, -2, 30, -23, -5, 6]
let contador = 0

while (contador < numeros.length) {
   
    if (numeros[contador] < 0 ) {
        console.log(numeros[contador])
    }
   contador++
}